
``` bash
make COMPILER=GCC cfsil4py_mod
export PYTHONPATH=/work/c01/c01/wendiliu/app/MUI/MUI-1.1/couplingFSILab/wrappers/Python
```

### Create python package (option 2)
``` bash
make COMPILER=GCC package
make pip-install # To install with pip the created package
```

python3 -c "import cfsil4py; cfsil4py.cfsil4py_mod.muiCouplingIQNILS()"